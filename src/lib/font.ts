import { Electrolize, Orbitron } from 'next/font/google';

export const accent100 = Orbitron({
	subsets: ['latin'],
});

export const accent200 = Electrolize({
	subsets: ['latin'],
	weight: ['400'],
});
